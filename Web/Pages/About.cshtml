@page
@model AboutModel
@{
    ViewData["Title"] = "About";
    ViewData["Description"] = "What Utah Course Compass is and where its numbers come from: the University of Utah's public class schedule and grade summary.";
    ViewData["Canonical"] = "/About";
}

<div class="page-head">
    <h1>About</h1>
</div>

<div class="stat-row four">
    <div class="stat">
        <span class="stat-value">@Model.Courses.ToString("N0")</span>
        <span class="stat-label">Courses</span>
    </div>
    <div class="stat">
        <span class="stat-value">@Model.Sections.ToString("N0")</span>
        <span class="stat-label">Sections</span>
    </div>
    <div class="stat">
        <span class="stat-value">@Model.Instructors.ToString("N0")</span>
        <span class="stat-label">Instructors</span>
    </div>
    <div class="stat">
        <span class="stat-value">@Model.GradedSections.ToString("N0")</span>
        <span class="stat-label">Graded sections</span>
    </div>
</div>
